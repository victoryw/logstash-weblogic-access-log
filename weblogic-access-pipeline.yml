input {
  file {
    path => "/picclife/weblogic/wlserver12.2.1.3/user_projects/domains/base_domain/logs/error_*"
    codec => multiline {
      pattern => "^\s"
      what => "previous"
    }
  }
}

filter {}

output {
    elasticsearch {
        hosts => ["10.137.193.59:9200", "10.137.193.60:9200","10.137.193.62:9200"]
        index => "cbs-local-error-%{+YYYY.MM.dd}"
        sniffing => true
        template_overwrite => true
    }
}

